const express = require('express'); const { createProxyMiddleware } = require('http-proxy-middleware'); const app = express(); const TARGET = 'https://www.twitch.tv'; app.use('/', createProxyMiddleware({ target: TARGET, changeOrigin: true, secure: true, cookieDomainRewrite: "", // Helps with login/session cookies onProxyRes: function (proxyRes) { // Remove security headers that block the site from loading in a proxy delete proxyRes.headers['content-security-policy']; delete proxyRes.headers['x-frame-options']; } })); app.listen(process.env.PORT || 8080);
